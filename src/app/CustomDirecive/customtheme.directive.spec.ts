import { CustomthemeDirective } from './customtheme.directive';

describe('CustomthemeDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomthemeDirective();
    expect(directive).toBeTruthy();
  });
});
